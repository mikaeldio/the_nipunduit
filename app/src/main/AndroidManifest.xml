<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nipunduit.tugasbesar">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:name="com.orm.SugarApp"
        android:allowBackup="true"
        android:icon="@drawable/thenipunduit"
        android:label="THE NIPUNDUIT"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="DATABASE"
            android:value="nipunduit.db" />
        <meta-data
            android:name="VERSION"
            android:value="2.0" />
        <meta-data
            android:name="QUERY_LOG"
            android:value="TRUE" />
        <meta-data
            android:name="DOMAIN_PACKAGE_NAME"
            android:value="com.nipunduit.tugasbesar" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".InfoKeuangan" />
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AturJamMakan" />
        <activity android:name=".KeuPerbulanActivity" />
        <activity android:name=".TambahPengeluaran" />
        <activity android:name=".ShowPengeluaranHariActivity"/>
    </application>

</manifest>